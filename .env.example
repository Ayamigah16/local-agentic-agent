# Copy this file to .env and fill in real values.
# .env is gitignored — tokens never get committed.
# main.py loads .env automatically at startup; mcp_servers.json can also
# reference these as ${VAR_NAME} in "url", "headers", or "env" fields.

# Direct API tools (agent/token_api_tools.py)
FIGMA_TOKEN=
VERCEL_TOKEN=
